// import resolve, { ResolvedVersions } from "version-resolver";

// let resolvedVersions: ResolvedVersions = null as any;
// export async function resolveDeps(deps: any): Promise<ResolvedVersions> {
//   return (resolvedVersions = resolvedVersions || (await resolve(deps)));
// }
